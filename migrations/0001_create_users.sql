CREATE TABLE IF NOT EXISTS users (id INTEGER PRIMARY KEY NOT NULL, language TEXT NOT NULL DEFAULT 'en' CHECK (language IN ('en', 'ru', 'uk')), created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP);
